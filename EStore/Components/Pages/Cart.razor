@page "/cart"

<PageTitle>LogiSale - Cart</PageTitle>

<div class="cart-page-container">
    <div class="cart-header mb-4">
        <h1>Your Shopping Bag</h1>
        <p class="text-secondary m-0 mt-1">Review your selected items before initiating secure checkout.</p>
    </div>

    <!-- Empty Cart UI, hidden by default -->
    <div id="empty-cart" class="empty-cart text-center py-5 section-card" style="display: none;">
        <svg class="empty-cart-icon mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
        </svg>
        <h3>Your Basket is Empty</h3>
        <p class="text-secondary mb-4">It looks like you haven't added any premium products yet.</p>
        <a href="products" class="btn btn-primary">
            Explore Our Collection
        </a>
    </div>

    <!-- Loading Spinner -->
    <div id="cart-loading" class="text-center py-5">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Loading your bag...</span>
        </div>
    </div>

    <!-- Cart Content Wrapper, populated and shown by JS -->
    <div id="cart-content-wrapper" class="row g-4" style="display: none;">
        <!-- Left Side: List of Items -->
        <div class="col-lg-8">
            <div id="cart-items-list" class="cart-items-list d-flex flex-column gap-3">
                <!-- Items are dynamically added here -->
            </div>
        </div>

        <!-- Right Side: Checkout Summary -->
        <div class="col-lg-4">
            <div class="summary-card section-card">
                <h3 class="summary-title mb-4">Order Summary</h3>
                
                <div class="summary-row d-flex justify-content-between mb-3">
                    <span class="text-secondary">Subtotal</span>
                    <span id="cart-subtotal" class="font-mono">$0.00</span>
                </div>
                <div class="summary-row d-flex justify-content-between mb-3">
                    <span class="text-secondary">Shipping & Taxes</span>
                    <span class="text-muted">Calculated at Checkout</span>
                </div>
                
                <hr class="summary-divider my-4" />

                <div class="summary-row d-flex justify-content-between align-items-center mb-4">
                    <span class="total-label">Estimated Total</span>
                    <span id="cart-total" class="total-val">$0.00</span>
                </div>

                <button id="checkout-btn" class="btn btn-primary btn-lg w-100 py-3 d-flex align-items-center justify-content-center gap-2" onclick="cart.checkout()">
                    <svg style="width: 20px; height: 20px;" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                    </svg>
                    Proceed to Secure Checkout
                </button>
                
                <div class="secure-checkout-notice text-center mt-3 d-flex align-items-center justify-content-center gap-1 text-muted">
                    <svg style="width: 14px; height: 14px;" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                    </svg>
                    <span>Secured Credit Card Payments</span>
                </div>
            </div>
        </div>
    </div>
</div>

<template id="cart-item-template">
    <div class="cart-item-card">
        <!-- Abstract Thumbnail -->
        <div class="cart-item-visual">
            <svg style="width: 24px; height: 24px; color: var(--accent);" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
            </svg>
        </div>

        <!-- Title & Description -->
        <div class="cart-item-details">
            <h3 class="cart-item-title text-truncate m-0"></h3>
            <p class="cart-item-desc text-secondary m-0 mt-1"></p>
        </div>

        <!-- Quantity Pill -->
        <div class="cart-item-qty">
            <span class="qty-label text-muted">QTY</span>
            <span class="qty-val"></span>
        </div>

        <!-- Pricing -->
        <div class="cart-item-price text-end">
            <span class="price-single text-muted"></span>
            <span class="price-total"></span>
        </div>

        <!-- Actions -->
        <div class="cart-item-action">
            <button class="btn-remove" title="Remove item">
                <svg style="width: 20px; height: 20px;" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-16v1a3 3 0 003 3h10M4 7h16" />
                </svg>
            </button>
        </div>
    </div>
</template>

<script>
    if (window.cart && typeof window.cart.renderCartPage === 'function') {
        window.cart.renderCartPage();
    }
</script>
